{
    "name": "kano-api-client",
    "version": "1.0.0",
    "description": "intercept calls between the server and the client try to minimise them",
    "main": "api-client.js",
    "directories": {
        "test": "test"
    },
    "scripts": {
        "install": "bower up",
        "test": "polymer test",
        "serve": "polymer serve",
        "lint": "./node_modules/.bin/eslint --fix api-client.js --global localStorage --global crypto --global performance --global Gun --global fetch"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "bower": "^1.8.4",
        "polymer-cli": "^1.6.0",
        "eslint": "^4.19.1",
        "eslint-config-kano": "^0.1.0",
        "eslint-plugin-html": "^4.0.3",
        "eslint-plugin-import": "^2.11.0"
    }
}
